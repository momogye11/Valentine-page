{
  "name": "morgane-invitation",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive private invitation with consent-based response tracking.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check analytics.js && node --check script.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0"
  }
}
